<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="OptiHub.Views.NvidiaPanelSettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:OptiHub.ViewModels"
    Title="OptiHub NVIDIA Panel"
    PrimaryButtonText="Fix all"
    CloseButtonText="Close"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ScrollViewer MaxHeight="540" VerticalScrollBarVisibility="Auto" Padding="0,4,8,0">
        <StackPanel Spacing="10" MinWidth="440" MaxWidth="500">
            <TextBlock Style="{StaticResource OptiCaption}"
                       Text="Driver-level settings OptiHub owns. Green = applied. Not applied means the driver is not on OptiHub policy — press Fix."
                       TextWrapping="Wrap" />

            <ProgressRing x:Name="LoadingRing"
                          Width="28" Height="28"
                          IsActive="True"
                          HorizontalAlignment="Center"
                          Margin="0,12" />

            <ItemsControl x:Name="PolicyList" Visibility="Collapsed">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:NvidiaPolicyRowViewModel">
                        <Border Style="{StaticResource OptiCard}"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Background="{ThemeResource OptiAccentSoftBrush}"
                                Opacity="{x:Bind Opacity}">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Width="32" Height="32"
                                        CornerRadius="9"
                                        Background="{ThemeResource OptiCardFillBrush}"
                                        BorderBrush="{ThemeResource OptiCardStrokeBrush}"
                                        BorderThickness="1"
                                        VerticalAlignment="Center">
                                    <FontIcon Glyph="{x:Bind Glyph}"
                                              FontSize="14"
                                              Foreground="{ThemeResource OptiPrimaryTextBrush}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Title}"
                                               FontFamily="{StaticResource OptiUiFontSemiBold}"
                                               FontSize="13.5"
                                               FontWeight="SemiBold"
                                               Foreground="{ThemeResource OptiPrimaryTextBrush}"
                                               TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Status}"
                                               FontFamily="{StaticResource OptiUiFont}"
                                               FontSize="12"
                                               Foreground="{ThemeResource OptiMutedTextBrush}"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                                <Button Grid.Column="2"
                                        Style="{StaticResource OptiQuietButton}"
                                        Content="Fix"
                                        MinWidth="72"
                                        VerticalAlignment="Center"
                                        Visibility="{x:Bind NeedsFix, Mode=OneWay}"
                                        Tag="{x:Bind Id}"
                                        Click="FixRow_Click"
                                        AutomationProperties.Name="Fix this NVIDIA policy" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="FooterText"
                       Style="{StaticResource OptiCaption}"
                       Text=""
                       TextWrapping="Wrap"
                       Margin="0,4,0,0" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
